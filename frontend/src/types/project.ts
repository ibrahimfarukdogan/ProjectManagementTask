export interface Project {
  id: number;
  project_name: string;
  start_date: string;
  finish_date: string;
}
export interface NewProject {
  project_name: string;
  start_date: string;
  finish_date: string;
}